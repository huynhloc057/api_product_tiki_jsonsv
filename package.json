{
  "name": "server",
  "version": "1.0.0",
  "description": "demo how to use json server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js server.js",
    "start": "npm run generate-data && node main.js",
    "generate-data": "node generate-data.js",
    "auth": "nodemon server.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Huynh Loc",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.0",
    "faker": "^5.5.3",
    "json-server": "^0.17.0",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
